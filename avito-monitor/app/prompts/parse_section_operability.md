Ты анализируешь объявление о продаже {{model}}. Извлеки состояние следующих признаков РАБОТОСПОСОБНОСТИ из текста объявления и полей Avito.

Признаки:
{{features_listing}}

Дополнительные указания по ключам:

- operability.parts_only (продаётся только на запчасти / на разбор):
  Intent-based detection — определи намерение продавца, а не состояние устройства.
  defect — explicit «на запчасти», «на разбор», «нерабочий, продаю как комплект», «только на детали», «разборка».
  ok — продавец продаёт как рабочее устройство (нет explicit указания «на запчасти»).
  unknown — нет явного указания ни в ту, ни в другую сторону.

ВАЖНО:
- Ставь "ok" ТОЛЬКО если продавец ЯВНО сказал, что признак в порядке.
- Ставь "defect" если продавец явно описал проблему.
- Если признак вообще не упомянут или сомнительно — "unknown".
- evidence = короткая цитата из объявления или null.

Заголовок: {{title}}

Описание:
"""
{{description}}
"""

Avito-параметры:
{{parameters_yaml}}

Ответь ОДНИМ JSON-объектом по схеме (без markdown, без пояснений):
{
  "<feature_key>": {"state": "ok|defect|unknown", "confidence": 0.0-1.0, "evidence": "цитата или null"},
  ...
}
