node_modules/
.env
ops/server/.env
*.key
*.pem
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Claude Code — local overrides (settings.json коммитится для команды)
.claude/settings.local.json

# Worktrees for isolated dev workspaces
.worktrees/

# Large binaries
*.apk
*.so
*.dex
*.pcap
*.mitm
avito-farm-agent/apk_work/
AvitoAll/dex_extract/
AvitoAll/lib_extract/

# APK reverse-engineering workspace (binaries + decompiled — never commit)
Reverse Avito/

# Syncthing tmp/conflict files
*~syncthing~*
*.sync-conflict-*

# Local scratch / shell snippets (e.g. accidentally-saved prompt commands)
claude --dangerously-skip-permissions
